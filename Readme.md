Mods that can't be added in github repository could be gotten by downloading then using the powershell script "getMods.ps1"
To run the script, right click on the file and choose "Run with PowerShell"